# SAT solver selection. Change only this line to switch solver after paths are set.
solver=kissat

# Uniform timeout for both solvers (seconds).
timeout_seconds=60

# Solver binaries. Use absolute paths or names available in PATH.
kissat_path=/home/dinah/kissat/build/kissat
minisat_path=/usr/bin/minisat
